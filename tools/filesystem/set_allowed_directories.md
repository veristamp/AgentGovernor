# filesystem.set_allowed_directories

> Update the list of allowed directories at runtime.

## Signature

```python
await set_allowed_directories(directories: list)
```

## Description

Update the list of allowed directories at runtime.

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `directories` | array | ✓ | - |

## Usage Example

```python
result = await filesystem_binding.set_allowed_directories(
    # Add parameters here
)
```
