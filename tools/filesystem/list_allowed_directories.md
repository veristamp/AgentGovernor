# filesystem.list_allowed_directories

> Returns the list of directories this server can access.

## Signature

```python
await list_allowed_directories()
```

## Description

Returns the list of directories this server can access.

## Usage Example

```python
result = await filesystem_binding.list_allowed_directories(
    # Add parameters here
)
```
