# filesystem.view-directory-ui

> Renders an interactive UI to display the contents of a directory.

## Signature

```python
await view-directory-ui(path: str)
```

## Description

Renders an interactive UI to display the contents of a directory.

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `path` | string | ✓ | Path to the directory |

## Usage Example

```python
result = await filesystem_binding.view-directory-ui(
    # Add parameters here
)
```
