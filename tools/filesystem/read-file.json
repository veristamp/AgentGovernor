{
	"qualifiedName": "filesystem.read-file",
	"serverPrefix": "filesystem",
	"name": "read-file",
	"originalName": "read-file",
	"description": "Read the complete contents of a file asynchronously.\n\nArgs:\n    path: Path to the file\n    encoding: \"utf-8\" for text files (default), \"base64\" for binary files (xlsx, images, pdf)\n\nFor binary files like Excel, use encoding=\"base64\" to get base64-encoded content.\nOnly works within allowed directories.",
	"schema": {
		"type": "object",
		"properties": {
			"path": {
				"type": "string",
				"description": "Path to the file"
			},
			"encoding": {
				"default": "utf-8",
				"description": "\"utf-8\" for text files (default), \"base64\" for binary files (xlsx, images, pdf)",
				"type": "string"
			}
		},
		"required": ["path"],
		"$schema": "http://json-schema.org/draft-07/schema#"
	}
}
