# memory.visualize_graph

> Visualize the knowledge graph as an interactive HTML UI.

## Signature

```python
await visualize_graph()
```

## Description

Visualize the knowledge graph as an interactive HTML UI.

## Usage Example

```python
result = await memory_binding.visualize_graph(
    # Add parameters here
)
```
